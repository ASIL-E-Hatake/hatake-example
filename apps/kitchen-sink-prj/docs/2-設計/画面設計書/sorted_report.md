# 帳票（sorted_report）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: 帳票
- データの出どころ: `lineRepository`

## 帳票（降順）（sorted_report）— 印刷向けの帳票

### データ

- データの出どころは lineRepository（アプリ側が用意する）。

### 絞り込める条件

- コード … 完全一致

### 印刷する列

- コード
- 名前
- 金額（¥1,234,567 のように見せる）

### 帳票の体裁

- 用紙は A4 の縦、1枚に 25 行
- itemCode の降順で並べて印刷する
- コード（itemCode）が変わるところで小計を出す
- 合計を出すのは lineAmount（合計）
- 1回に取るのは 200 行まで

### できる操作

- CSV 出力 … 内容をファイルに出す。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。admin だけに出る
- 印刷 … 紙に刷る。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。admin だけに出る

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「帳票（降順）」（メニュー） … 誰でも通れる

### 画面の中で隠れるもの（権限）

- ボタン「CSV 出力」 … admin だけ
- ボタン「印刷」 … admin だけ

### この画面でできないこと

- 画面から書き換えはできない

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
