# ステップ入力（steps_wizard）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: ウィザード（ステップ入力）
- データの出どころ: `itemRepository`
- 1件を指すキー: `itemCode`

## ステップ入力（steps_wizard）— 入力をステップに分けた画面

### データ

- データの出どころは itemRepository（アプリ側が用意する）。
- 1件を指すキーは itemCode。

### 入力の順番

- 1. 1つ目（2 項目） — 種別を選ぶと、次のステップが出るかどうかが変わります。
- 2. 特別のときだけ（種別 が 特別 のときだけ出る枠）（1 項目）
- 3. 確認（1 項目）

### 1つ目の項目

- コード … 必須
- 種別 … 選択、必須、選べるのは 標準 / 特別

### 特別のときだけ（種別 が 特別 のときだけ出る枠）の項目

- 名前 … 必須

### 確認の項目

- 内容 … 他の項目から自動で計算する（手では入れない）

### できる操作

- 保存 … アプリ側の処理を呼ぶ（saveItem）。失敗したら理由がそのまま出る（業務の言葉にするなら onError）
- やめる … 別の画面へ移る（press_list へ）

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「ステップ入力」（メニュー） … 誰でも通れる

### この画面でできないこと

- 途中では保存しない（最後にまとめて1回）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
