# 畳み込み（fold_detail）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: 詳細（読み取り）
- データの出どころ: `itemRepository`
- 1件を指すキー: `itemCode`

## 畳み込み（fold_detail）— 1件の内容を読むだけの画面

### データ

- データの出どころは itemRepository（アプリ側が用意する）。
- 1件を指すキーは itemCode。

### もとの1件

- コード
- 名前
- 種別

### 入力する項目

- 明細 … 明細（表で複数行）、1行は 名前・金額、行はこのレコードと一緒に保存する

### 畳んだ結果

- 金額の大きい順に3件 … 複数行、読み取り専用、明細 の lineName を並べたもの（手では入れない）、lineAmountが大きい順に3 件だけ、切ったぶんは件数で言う
- 合計 … 明細 の lineAmount の合計（手では入れない）、¥1,234,567 のように見せる

### できる操作

- 一覧へ戻る … 別の画面へ移る（press_list へ）

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「詳細」（press_list から） … 誰でも通れる

### この画面でできないこと

- この画面では書き換えられない（読み取り専用）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
