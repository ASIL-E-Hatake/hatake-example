仕分けた行は 12 件。**うち 2 件は枠組みの外**（当てられなかった行が 20 件）。

・データは決め打ち（DB を持たない）。保存しても残らない
    [枠組みの外（hatake は持たない）] データベース
      hatake は持たない。Repository の向こうで自由に選ぶ（RDB でも API でもファイルでも、枠組みからは同じ）。
    [サーバの担当] 実際に保存する（トランザクション・排他・採番）
      Repository の向こう側で自分でやる。画面は「保存してくれ」と言うだけ。
    （ほかに 1 件当たっています）

・認証は持たない。役割は URL の `?role=tester,admin` で切り替える
    [枠組みの外（hatake は持たない）] 認証（ログイン・トークンの発行）
      hatake は持たない。ログイン画面は定義で作れるが、資格を確かめるのはサーバか認証基盤。`hatake_http` は**毎回ヘッダを聞く**口だけ持つ（トークンは期限が切れるので）。
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・> **これは人が書いた紙です。** ただし業務の案件ではありません ──
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

・*業務らしさが要る見本は別に2本ある**（マスタメンテ・受注入力）。こちらはそれとは
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・役割が違う。
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・| 条件の組み合わせ | `all` / `any` / `not` と入れ子、既定値 |
    [定義で書ける] 条件で絞り込む
      書くキー: search / filters / operator / optionsFrom
    [定義で書ける] 条件で出し分ける（見せる・必須にする・押せる）
      書くキー: visibleWhen / requiredWhen / readOnlyWhen / enabledWhen

・| 押す前に聞く | 押す前に値を聞く・区切って実行・行ごとの有効条件・別タブ・成功後に移動 |
    [定義で書ける] 条件で絞り込む
      書くキー: search / filters / operator / optionsFrom
    [定義で書ける] 条件で出し分ける（見せる・必須にする・押せる）
      書くキー: visibleWhen / requiredWhen / readOnlyWhen / enabledWhen
    （ほかに 1 件当たっています）

・| 選択肢の連動 | 親で子を絞る・取る数の上限・ページ送りを切る |
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・| 畳み込み | 並べてから上位だけ畳む・切ったことを言う・詳細のボタン |
    [アプリ側に登録して足す] ボタンで独自の処理をする（CSV・外部連携・独自の保存）
      書くキー: plugin

・| ステップ入力 | ウィザードのボタン・条件で飛ばすステップ |
    [アプリ側に登録して足す] ボタンで独自の処理をする（CSV・外部連携・独自の保存）
      書くキー: plugin
    [定義で書ける] 条件で絞り込む
      書くキー: search / filters / operator / optionsFrom
    （ほかに 2 件当たっています）

・| 帳票（降順） | 降順で刷る・持ち出しは役割で絞る |
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・どこまで自動で合否にするか（画面の見た目は目で見る前提でよいか）
    [アプリ側に登録して足す] 無いフィールド型・見た目を足す
      書くキー: type

当てられなかった行（**表に無いだけかもしれない**ので、人が見ること）:
  ・機能網羅（人が最初に書いたメモ）
  ・> この見本の「案件」は、**DSL で書けることを全部いちど書いて動かすこと**そのものです。
  ・何のために作るか
  ・**1.0 で凍らせる前に、全部いちど動かしておく**。
  ・一度も動かしたことのないキーを凍らせるのが、いちばん高くつく間違いなので。
  ・**版を上げたときの影響を1か所で見る**。出力（読み返し・設計資料・値・画面）を
  ・固定してあるので、`hatake.version` を上げて回すと**差分がそのまま移行手順書**になる。
  ・業務のふりはしない
  ・納品物の見本ではない。人に見せる設計書としての値打ちは狙わない
  ・画面は「確かめたいこと」で分ける
  ・業務で分けると、落ちたときにどこが悪いのか分からない。
  ・| 画面 | 確かめたいこと |
  ・|---|---|
  ・網羅できているかは、機械が数える
  ・`spec/reference.json` の **(ノード, キー) 249組**を台帳にして、
  ・`node tool/check-coverage.mjs` が「まだ一度も書かれていない所」を数える。
  ・*書けないものは理由つきで除外表に書く。** 理由を書かせないと、
  ・「たまたま漏れた」と「意図して外した」が区別できず、凍結の判断に使えない。
  ・決まっていないこと
  ・版を上げたときの差分を、どの粒度で追いかけるか

※ 仕分けは**下書き**です（当てているのは言葉の一致だけ）。決めるのは人で、外と出た行は**その場で書き始めずに**依頼した人に返してください。
※ ここは**担当の表**です（何ができるかの一覧ではありません）。「枠組みの外」と出たものは hatake では書けません＝画面には**結果だけ**を出し、判断はサーバか別のシステムに置いてください（勝手に Dart / TypeScript を書き始めない）。
