仕分けた行は 20 件。**うち 3 件は枠組みの外**（当てられなかった行が 23 件）。

・ログインは既存の社内ポータルの ID を使う（そちらが役割も返してくれる）。
    [枠組みの外（hatake は持たない）] 認証（ログイン・トークンの発行）
      hatake は持たない。ログイン画面は定義で作れるが、資格を確かめるのはサーバか認証基盤。`hatake_http` は**毎回ヘッダを聞く**口だけ持つ（トークンは期限が切れるので）。
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・締めた月の受注は直せないようにしたい
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。

・締め処理を誰がやるか
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。

・受注入力（人が最初に書いたメモ）
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・> **これは人が書いた紙です。** AI に渡す前に、人が知っていることを普通の日本語で
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

・いまは Excel の注文書を手で転記していて、単価の入れ間違いと消費税の計算違いが毎月出る。
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where

・| 役割 | できること |
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・| 管理者（manager） | 全部。ダッシュボードと帳票も見る |
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage
    [定義で書ける] カードを並べる（件数・金額・グラフ）
      書くキー: items / chart / value / span

・受注入力の画面
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・「明細（商品・数量・単価・金額）」でできている。明細は1〜20行くらい。
    [定義で書ける] 見せ方を決める（金額・和暦・マスク）
      書くキー: format / config

・受注番号は**サーバが採番**する（画面では触らせない）
    [サーバの担当] 実際に保存する（トランザクション・排他・採番）
      Repository の向こう側で自分でやる。画面は「保存してくれ」と言うだけ。

・明細の金額 = 数量 × 単価（**画面で自動計算**。手で入れさせない）
    [定義で書ける] 見せ方を決める（金額・和暦・マスク）
      書くキー: format / config
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where

・小計 = 明細の金額の合計
    [定義で書ける] 見せ方を決める（金額・和暦・マスク）
      書くキー: format / config
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where
    （ほかに 1 件当たっています）

・消費税 = 小計 × 税率（標準10%。軽減8%の商品が混ざることがある）
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

・合計 = 小計 + 消費税
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

・同じ商品を2行に分けて入れてしまうミスが多いので、**重複を弾きたい**
    [サーバの担当] 実際に保存する（トランザクション・排他・採番）
      Repository の向こう側で自分でやる。画面は「保存してくれ」と言うだけ。

・受注の登録はステップで進めたい
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・**受注照会**: 受注番号・取引先・受注日の範囲・状態で探す。一覧から詳細へ
    [定義で書ける] 一覧を出す（並べ替え・ページング・列の幅）
      書くキー: table / columns / sortable / pagination / width

・**ダッシュボード**: 今月の受注件数・受注金額・取引先別の上位・日別の推移
    [定義で書ける] 見せ方を決める（金額・和暦・マスク）
      書くキー: format / config
    [定義で書ける] カードを並べる（件数・金額・グラフ）
      書くキー: items / chart / value / span

・**注文請書（帳票）**: 1件を A4 で刷って取引先に送る。明細と合計が出る
    [定義で書ける] 画面の中で計算する（小計・合計・差）
      書くキー: computed / aggregate / of / where
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

当てられなかった行（**表に無いだけかもしれない**ので、人が見ること）:
  ・> 書き出しただけのもの。ここを AI に書かせてはいけない。
  ・どんなシステムか
  ・営業事務が電話・FAX で受けた注文を入れて、出荷指示まで出す画面。
  ・利用者は営業所3拠点で15人くらい。繁忙期は1日200件入る。
  ・誰が使うか
  ・|---|---|
  ・| 営業（sales） | 受注を入れる・直す。自分の拠点のぶんだけ |
  ・| 営業事務（clerk） | 全拠点の受注を入れる・直す・取り消す |
  ・1件の受注は「ヘッダ（受注番号・取引先・受注日・納期・担当）」と
  ・納期は受注日以降
  ・数量は1以上
  ・新人が多いので、いきなり全部の欄が出ると迷う。
  ・「取引先を選ぶ → 明細を入れる → 確認して確定」の3ステップにしたい。
  ・そのほかの画面
  ・**受注詳細**: 1件を読むだけ（明細も見える）
  ・やりたいこと（画面以外も含む）
  ・出荷指示は基幹システムに流す（こちらからは投げるだけ）
  ・単価は取引先ごとの契約単価を使いたいが、**今回は商品マスタの定価でよい**
  ・取り消した受注も残す（消さない）
  ・決まっていないこと
  ・消費税の端数（切り捨て？　四捨五入？）
  ・同時に同じ受注を直したときにどうするか
  ・出荷指示を投げるタイミング

※ 仕分けは**下書き**です（当てているのは言葉の一致だけ）。決めるのは人で、外と出た行は**その場で書き始めずに**依頼した人に返してください。
※ ここは**担当の表**です（何ができるかの一覧ではありません）。「枠組みの外」と出たものは hatake では書けません＝画面には**結果だけ**を出し、判断はサーバか別のシステムに置いてください（勝手に Dart / TypeScript を書き始めない）。
