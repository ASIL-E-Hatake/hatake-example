# 受注照会（order_search）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: 照会（検索・一覧）
- データの出どころ: `orderRepository`
- 1件を指すキー: `orderNo`

## 受注照会（order_search）— 検索して一覧を見るだけの画面

### データ

- データの出どころは orderRepository（アプリ側が用意する）。
- 1件を指すキーは orderNo。

### 絞り込める条件

- 受注番号 … 完全一致
- 取引先 … 完全一致。選択肢は customerRepository から引く
- 受注日 … 期間・範囲（開始と終了の2つ）
- 納期 … 期間・範囲（開始と終了の2つ）
- 受注状態 … いずれかに一致。選べるのは 入力中 / 確定 / 出荷済 / 取消
- 合計 … 期間・範囲（開始と終了の2つ）

<details>
<summary><b>一覧に出る列</b>（9 件）</summary>

- 受注番号（並べ替えできる）
- 取引先（並べ替えできる）
- 受注日（並べ替えできる）
- 納期（並べ替えできる）
- 受注状態
- 明細
- 合計（¥1,234,567 のように見せる、並べ替えできる）
- 入力者
- 50 件ずつページングする

</details>

### できる操作

- 詳細 … 別の画面へ移る（order_detail へ）
- 修正 … 別の画面へ移る（order_entry へ）
- まとめて取り消す … アプリ側の処理を呼ぶ（bulkCancel）。選んだ行に対して実行する（1回で最大 50 件まで（clerk は 20 件）＝定義で決めた上限）。押すと確認を出す。終わったら「{count} 件を取消にしました」と出す。失敗したら「{count} 件が取り消せませんでした（失敗した行: {failedKeys}）」と出す。clerk だけに出る
- CSV 出力 … 内容をファイルに出す。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。clerk / manager だけに出る

### 行ごとの操作（一覧の各行に出る）

- 詳細
- 修正

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「受注照会」（メニュー） … 誰でも通れる
- 入口「受注照会」（order_dashboard から） … 誰でも通れる

### 画面の中で隠れるもの（権限）

- 列「入力者」 … clerk / manager だけ
- ボタン「まとめて取り消す」 … clerk だけ
- ボタン「CSV 出力」 … clerk / manager だけ

### この画面でできないこと

- 登録・修正・削除はできない（照会専用）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
