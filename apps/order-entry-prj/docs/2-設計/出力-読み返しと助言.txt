definitions/app.yaml（app: 5 ページ）を1回で見ました。

## 事実（書いたのに効かない）
登録済みの一覧を見ました（渡された一覧）。繋がっていない所を1枚で見るなら hatake gaps。
警告はありません。

## 読み返し（そう書いてある）
受注管理（order_entry）— 画面 5 枚

  order_search     受注照会            照会（読み取り専用）。条件 6、列 8、ボタン 4、権限で出し分けあり、orderRepository から
  order_entry      受注入力            段階入力。ステップ 3（項目 12）、orderRepository から
  order_detail     受注詳細            1件の照会。4 枠に項目 13、権限で出し分けあり、orderRepository から
  order_dashboard  受注ダッシュボード  数字とグラフ。条件 2、カード 7、ボタン 1、orderRepository から
  order_slip       注文請書            帳票。条件 2、列 6、ボタン 2、権限で出し分けあり、orderLineRepository から

## 好み（書いていないと不便かも）
書き足すと良さそうな所が 1 件:
  app.pages[1].steps.2.fields.4 [project-glossary-name]
    こうなる: ラベルは「商品」の話なのに、名前 "orderSummary" に用語辞書の名前（productCode）の語 "product" が入っていません。
    書き足す: 辞書の名前に合わせる（「商品」は `productCode`）。別の属性なら "product" で始まる名前にする。
定義の中の印（advise-off）で 2 件を黙らせました（印 1 件: total-without-compare／order_detail）。
※ ここは**助言**（書いていないから不便かもしれない所）で、警告ではありません。業務によっては要らないものもあるので、終了コードは変えません。「書いたのに効かない」は hatake validate が見ます。

## 人が決めること（定義に書けない）
決めていないことは見つかりませんでした。前書きで答えが済んでいるもの 11件。

※ この道具が見るのは **12 種類だけ**です。出なかった＝決まっている、ではありません（表に無いことは聞きません）。**終了コードは変えません**（問いは人への依頼で、機械の合否ではないので）。答えが決まったら、案件の前書きの `logic` にその1行を足して `answers: [<印>]` を付けてください（既定のままでよいと決めたなら `questions.decided` に理由つきで）＝次からその問いは出ません。

## 入れなかった欄
  読み返しの全文 … 画面が 5 枚あるので1行ずつにしました（画面を1枚に絞ると、その画面の全文になります）。

## 次の1手
  ・好みが 1 件（書いていないと不便かも）。当てるなら hatake advise --apply、案件で要らないと決めたなら物差しの off か定義の印（# advise-off:）。**直さなくても CI は赤くなりません**。

※ この紙は hatake validate / explain / advise / ask を1回で回したものです（**欄は混ぜていません**＝終了コードを動かすのは事実の欄だけ）。**動かしてはいません**＝その値でいくらになるか・押せるかは hatake run の担当、画面の外との辻褄（登録していない Repository やプラグイン）は登録済みの一覧を渡したときだけ見ます。人に渡す1枚が要るなら hatake design（あちらは意図の1枚と突き合わせる紙で、合否は言いません）。
