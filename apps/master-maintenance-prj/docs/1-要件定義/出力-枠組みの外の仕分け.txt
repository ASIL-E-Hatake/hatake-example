仕分けた行は 14 件。**うち 7 件は枠組みの外**（当てられなかった行が 28 件）。

・ログインは社内の Active Directory を使いたいが、今回は間に合わないので
    [枠組みの外（hatake は持たない）] 認証（ログイン・トークンの発行）
      hatake は持たない。ログイン画面は定義で作れるが、資格を確かめるのはサーバか認証基盤。`hatake_http` は**毎回ヘッダを聞く**口だけ持つ（トークンは期限が切れるので）。

・*ID とパスワードをこのシステムで持つ**。将来 AD に差し替える。
    [枠組みの外（hatake は持たない）] 認証（ログイン・トークンの発行）
      hatake は持たない。ログイン画面は定義で作れるが、資格を確かめるのはサーバか認証基盤。`hatake_http` は**毎回ヘッダを聞く**口だけ持つ（トークンは期限が切れるので）。

・インボイス登録番号・締め日・支払サイト・与信限度額・取引状態。
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout

・与信限度額は金額（カンマ区切りで見せたい）
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。
    [定義で書ける] 見せ方を決める（金額・和暦・マスク）
      書くキー: format / config

・検索が複雑: 取引先名の部分一致、区分、取引状態、与信限度額の範囲、
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。
    [定義で書ける] 条件で絞り込む
      書くキー: search / filters / operator / optionsFrom

・締め日の複数選択、「与信限度額が0のものだけ」みたいな絞り込みもしたい
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。
    [定義で書ける] 条件で絞り込む
      書くキー: search / filters / operator / optionsFrom
    （ほかに 1 件当たっています）

・与信限度額の端数
    [枠組みの外（hatake は持たない）] 業務ロジック（締め・引当・与信・按分）
      hatake は持たない。画面に出るのは**その結果**だけ＝「締めたら直せない」は `readOnlyWhen` で見せ、判断そのものはサーバか別のシステムに置く。案件の前書きの `premises` に「できないこと」として書いておくと、AI が画面側に実装しない。

・> **これは人が書いた紙です。** AI に渡す前に、人が知っていることを普通の日本語で書き出した
    [定義で書ける] 帳票を出す（見出し・小計・紙のサイズ）
      書くキー: report / paper / groupBy / totals / rowsPerPage

・| 役割 | できること |
    [定義で書ける] 役割で見せ方を変える（表示の出し分けだけ）
      書くキー: roles

・退職者も残す（消さない）。一覧では既定で**在籍者だけ**を出す
    [定義で書ける] 一覧を出す（並べ替え・ページング・列の幅）
      書くキー: table / columns / sortable / pagination / width

・法人のときだけインボイス登録番号を必須にしたい
    [定義で書ける] 入力の枠を作る（新規・編集・ステップ）
      書くキー: form / sections / fields / steps / layout
    [定義で書ける] 入力を検証する（1項目・項目間・行どうし）
      書くキー: validators / required / requiredWhen

・一覧から CSV に落として Excel で確認したい
    [アプリ側に登録して足す] ボタンで独自の処理をする（CSV・外部連携・独自の保存）
      書くキー: plugin
    [定義で書ける] 一覧を出す（並べ替え・ページング・列の幅）
      書くキー: table / columns / sortable / pagination / width

・退職者をまとめて「退職」にする一括処理がほしい（人事のみ）
    [定義で書ける] 選んだ行にまとめて実行する
      書くキー: scope / maxRows / batchSize / confirm / prompt

・社員番号の採番を誰がやるか（人事が手で決める？　自動？）
    [サーバの担当] 実際に保存する（トランザクション・排他・採番）
      Repository の向こう側で自分でやる。画面は「保存してくれ」と言うだけ。

当てられなかった行（**表に無いだけかもしれない**ので、人が見ること）:
  ・社内マスタメンテナンス（人が最初に書いたメモ）
  ・> だけのもの。ここを AI に書かせてはいけない（案件のことは人しか知らない）。
  ・どんなシステムか
  ・人事部と購買部が使う、社内マスタの管理画面。いまは Excel で管理していて、
  ・更新のたびに情シスに依頼が来ている。これを自分たちで直せるようにしたい。
  ・利用者は社内だけ。同時に使うのは10人くらい。
  ・誰が使うか
  ・|---|---|
  ・| 情シス（admin） | 全部のマスタを見る・直す |
  ・| 人事（hr） | 社員マスタと部署マスタを見る・直す。取引先は見るだけ |
  ・| 一般（viewer） | どのマスタも**見るだけ** |
  ・管理したいマスタ
  ・社員マスタ
  ・社員番号・氏名・カナ・所属部署・役職・入社日・退職日・メール・内線・在籍区分。
  ・社員番号は6桁の数字。あとから変えられない
  ・入社日 ≤ 退職日
  ・部署マスタ
  ・部署コード・部署名・略称・上位部署・有効期間（開始日・終了日）。
  ・部署コードは英数4文字
  ・上位部署は部署マスタから選ぶ
  ・取引先マスタ
  ・取引先コード・取引先名・カナ・区分（法人／個人）・郵便番号・住所・電話・
  ・やりたいこと（画面以外も含む）
  ・部署マスタは組織図としても見たい
  ・監査のため「誰がいつ何を直したか」を残したい
  ・決まっていないこと
  ・同時に同じレコードを直したときにどうするか
  ・退職者の扱い（消す？　残す？）── たぶん残す

※ 仕分けは**下書き**です（当てているのは言葉の一致だけ）。決めるのは人で、外と出た行は**その場で書き始めずに**依頼した人に返してください。
※ ここは**担当の表**です（何ができるかの一覧ではありません）。「枠組みの外」と出たものは hatake では書けません＝画面には**結果だけ**を出し、判断はサーバか別のシステムに置いてください（勝手に Dart / TypeScript を書き始めない）。
