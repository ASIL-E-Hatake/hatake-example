# 部署マスタ（department_master）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: マスタ保守（CRUD と同じ形）
- データの出どころ: `departmentRepository`
- 1件を指すキー: `departmentCode`

## 部署マスタ（department_master）— マスタをメンテナンスする画面（検索・一覧・登録・修正・削除）

### データ

- データの出どころは departmentRepository（アプリ側が用意する）。
- 1件を指すキーは departmentCode。

### 絞り込める条件

- 部署名 … 部分一致

### 一覧に出る列

- 部署コード（並べ替えできる）
- 部署名（並べ替えできる）
- 略称
- 上位部署
- 開始日
- 終了日
- 50 件ずつページングする

### 部署

- 部署コード … 必須、決まった書式
- 部署名 … 必須
- 略称
- 上位部署 … 選択、選択肢は departmentRepository から引く
- 開始日 … 日付、必須
- 終了日 … 日付、開始日 以上

### できる操作

- CSV 出力 … 内容をファイルに出す。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。admin / hr だけに出る

### 行ごとの操作（一覧の各行に出る）

- 編集を開く
- 削除する

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「部署マスタ」（メニュー） … 誰でも通れる

### 画面の中で隠れるもの（権限）

- ボタン「CSV 出力」 … admin / hr だけ

### この画面でできないこと

- 新規登録のボタンは無い（入力画面は他から開く）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
