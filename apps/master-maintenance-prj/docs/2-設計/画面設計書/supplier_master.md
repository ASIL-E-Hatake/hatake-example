# 取引先マスタ（supplier_master）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: CRUD（検索・一覧・登録・修正・削除）
- データの出どころ: `supplierRepository`
- 1件を指すキー: `supplierCode`

## 取引先マスタ（supplier_master）— 検索して一覧に出し、その場で登録・修正・削除までできる画面

### データ

- データの出どころは supplierRepository（アプリ側が用意する）。
- 1件を指すキーは supplierCode。

### 絞り込める条件

- 取引先名 … 部分一致
- 区分 … 完全一致。選べるのは 法人 / 個人
- 取引状態 … 完全一致。選べるのは 取引中 / 停止 / 終了
- 与信限度額 … 期間・範囲（開始と終了の2つ）
- 締め日 … いずれかに一致。選べるのは 10日 / 15日 / 20日 / 末日

### 一覧に出る列

- 取引先コード（並べ替えできる）
- 取引先名（並べ替えできる）
- 区分
- 与信限度額（¥1,234,567 のように見せる、並べ替えできる）
- 締め日
- 支払サイト
- 取引状態
- 50 件ずつページングする

### 基本情報

- 取引先コード … 必須、10 文字以内
- 取引先名 … 必須
- カナ
- 区分 … 選択、必須、選べるのは 法人 / 個人
- インボイス登録番号 … 区分 が 法人 のときだけ必須、決まった書式

### 連絡先

- 郵便番号 … 郵便番号の形
- 住所
- 電話

### 取引条件

- 与信限度額 … 数値、必須、0 以上、¥1,234,567 のように見せる
- 締め日 … 選択、必須、選べるのは 10日 / 15日 / 20日 / 末日
- 支払サイト（日） … 数値、0 以上
- 取引状態 … 選択、必須、選べるのは 取引中 / 停止 / 終了

### できる操作

- CSV 出力 … 内容をファイルに出す。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。admin だけに出る

### 行ごとの操作（一覧の各行に出る）

- 編集を開く

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「取引先マスタ」（メニュー） … 誰でも通れる

### 画面の中で隠れるもの（権限）

- ボタン「CSV 出力」 … admin だけ

### この画面でできないこと

- 新規登録のボタンは無い（入力画面は他から開く）
- 削除はできない（削除のボタンが無い）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
