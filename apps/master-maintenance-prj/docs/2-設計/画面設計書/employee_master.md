# 社員マスタ（employee_master）

> **この紙は生成物です。** `tools/build-design-docs.mjs` が
> [定義](../../../definitions/app.yaml)から起こしています（手で直さない）。
> 定義を直したら `node tools/build-design-docs.mjs` で作り直してください。
>
> **この画面が何をするか**を、DSL のキー名を出さずに書いたもの。

- 種別: CRUD（検索・一覧・登録・修正・削除）
- データの出どころ: `employeeRepository`
- 1件を指すキー: `employeeNo`

## 社員マスタ（employee_master）— 検索して一覧に出し、その場で登録・修正・削除までできる画面

### データ

- データの出どころは employeeRepository（アプリ側が用意する）。
- 1件を指すキーは employeeNo。

### 絞り込める条件

- 社員番号 … 完全一致
- 氏名 … 部分一致
- 所属部署 … 完全一致
- 在籍区分 … 完全一致。選べるのは 在籍 / 休職 / 退職

<details>
<summary><b>一覧に出る列</b>（9 件）</summary>

- 社員番号（並べ替えできる）
- 氏名（並べ替えできる）
- カナ
- 所属部署
- 役職
- 入社日（並べ替えできる）
- 在籍区分
- 内線
- 50 件ずつページングする

</details>

### 基本情報

- 社員番号 … 必須、決まった書式
- 氏名 … 必須
- カナ … 必須
- 所属部署 … 選択、必須
- 役職
- メール … メールアドレスの形
- 内線

### 在籍

- 在籍区分 … 選択、必須、選べるのは 在籍 / 休職 / 退職
- 入社日 … 日付、必須
- 退職日 … 日付、在籍区分 が 退職 のときだけ必須、入社日 以上

### できる操作

- CSV 出力 … 内容をファイルに出す。失敗したら理由がそのまま出る（業務の言葉にするなら onError）。admin / hr だけに出る
- まとめて退職にする … アプリ側の処理を呼ぶ（bulkRetire）。選んだ行に対して実行する（1回で最大 50 件まで（hr は 20 件）＝定義で決めた上限）。押すと確認を出す。終わったら「{count} 件を退職にしました」と出す。失敗したら「{count} 件が処理できませんでした（失敗した行: {failedKeys}）」と出す。admin / hr だけに出る
- 詳細 … 別の画面へ移る（employee_detail へ）

### 行ごとの操作（一覧の各行に出る）

- 編集を開く
- 削除する

### この画面を開ける人

- 開けるのは … 誰でも開ける
- 入口「社員マスタ」（メニュー） … 誰でも通れる

### 画面の中で隠れるもの（権限）

- 列「内線」 … admin / hr だけ
- ボタン「CSV 出力」 … admin / hr だけ
- ボタン「まとめて退職にする」 … admin / hr だけ

### この画面でできないこと

- 新規登録のボタンは無い（入力画面は他から開く）

---

関連: [画面一覧](../画面一覧.md) / [画面遷移図](../画面遷移図.md) / [権限マトリクス](../権限マトリクス.md) / [API 一覧](../API一覧.md)
