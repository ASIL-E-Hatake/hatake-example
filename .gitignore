# Dart / Flutter
.dart_tool/
build/
pubspec.lock
.flutter-plugins*

# Java / Gradle
# ビルドの置き場（`gradle compileJava` を手元で回すと出る）。
# `build/` は上の Dart の行で既に落ちている。
.gradle/

# Node
node_modules/
dist/

# Docker が作る置き場
.data/

# エディタ
.idea/
.vscode/
*.iml

# テストが作るもの（生成物だが、納品物なので**残す**）
# ※ スクリーンショットは docs/4-テスト/画面/ に入る（.gitignore しない）
